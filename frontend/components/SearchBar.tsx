import { CiSearch } from "react-icons/ci";

const SearchBar = () => {
  return (
    <div>
      <CiSearch />
      <input type="text" placeholder="search" />
    </div>
  );
};

export default SearchBar;
